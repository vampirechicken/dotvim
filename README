Installation:

   git clone gitL//github.com/vampirechicken/dotvim.got ~/.vim

Create symlinks:

    ln -z ~/.vim/vimrc ~/.vimrc
    ln -z ~/.vim/gvimrc ~/.gvimrc

Initialize plugin bundles:

    cd ~/.vim
    git submodule update --init

Update plugins:

   cd ~/.vim/bundle
   git submodule foreach git pull origin master


